#include "1008_construct-binary-search-tree-from-preorder-traversal.h"

TEST(S, Demo) {
  Solution s;
  //{
  //  vector<int> v({8, 5, 1, 7, 10, 12});
  //  s.bstFromPreorder(v);
  //}
  {
    vector<int> v({4, 2});
    s.bstFromPreorder(v);
  }
}
