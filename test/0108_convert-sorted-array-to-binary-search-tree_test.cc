#include "0108_convert-sorted-array-to-binary-search-tree.h"

TEST(S, Demo) {
  Solution s;
  // s.sortedArrayToBST(vector<int>({3, 9, 20, 15, 7}));
  s.sortedArrayToBST(vector<int>({-10, -3, 0, 5, 9}));
  //
}
